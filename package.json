{
  "name": "skgateway",
  "version": "0.1.0",
  "description": "Enterprise AI inference proxy — routes, throttles, audits and observes AI API traffic",
  "type": "module",
  "main": "src/index.mjs",
  "bin": {
    "skgateway": "bin/skgateway.mjs"
  },
  "scripts": {
    "start": "node src/index.mjs",
    "dev": "node --watch src/index.mjs",
    "sync:pi-models": "node scripts/sync-pi-models.mjs",
    "test": "node --test --import ./tests/_setup.mjs tests/*.test.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.1",
    "js-yaml": "5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "license": "MIT"
}
