node_modules/
data/
logs/
*.db
*.db-journal
.env
__pycache__/
*.pyc
.coverage
.pytest_cache/
.ruff_cache/
*.egg-info/
node_modules

# Live deployment state and credentials. secrets/codex-auth.json is a synced copy
# of the Codex CLI credential (mode 0600) and was NOT ignored, so a blind
# "git add -A" in this checkout would have committed it.
secrets/
config/skgateway-codex.yaml
config/registry.yaml
*.bak-*
*.pre-*
